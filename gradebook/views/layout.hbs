<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
  </head>
  <h1>
  	{{#if user}}
		<div>You're currently logged in as: <a href=/users/{{user.username}}>{{user.username}}</a></div>
		{{else}}
		<div>
		<a href="/login">login</a> or 
		<a href="/register">register</a>
		</div>
		{{/if}}
  </h1>
  <body>
		
		<div style="width:50%; margin: auto; background-color:#ddf;padding:10px; border:solid 5px;">
			{{{body}}}
		</div>
  </body>
</html>
